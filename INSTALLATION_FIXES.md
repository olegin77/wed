# Исправления для auto-install-droplet.sh

## Обзор исправлений

Данный документ описывает все исправления, внесенные в скрипт `auto-install-droplet.sh` и связанные файлы для устранения критических ошибок при автоматической установке.

## Исправленные проблемы

### ✅ 1. Несоответствие паролей БД (КРИТИЧНО)

**Проблема:**
- В `docker-compose.yml` пароль базы данных был жестко закодирован как `pg`
- В скрипте установки генерировался случайный пароль
- Это приводило к невозможности подключения к базе данных

**Решение:**
- В `docker-compose.yml` изменены все строки подключения на использование переменной окружения:
  ```yaml
  DATABASE_URL: postgresql://pg:${DB_PASSWORD:-pg}@db:5432/wt
  ```
- В скрипте установки добавлена переменная `DB_PASSWORD` в `.env` файл
- Значение по умолчанию `pg` сохранено для обратной совместимости

**Файлы:**
- `docker-compose.yml` (строки 6, 42, 65, 88, 111, 134, 157, 180, 201)
- `scripts/auto-install-droplet.sh` (строка 309)

---

### ✅ 2. Проверка занятых портов

**Проблема:**
- Скрипт не проверял, заняты ли необходимые порты перед запуском docker-compose
- Это приводило к ошибкам "port already in use"

**Решение:**
- Добавлена функция `check_ports_available()` в скрипт
- Проверяются все необходимые порты: 3000, 3001-3007, 5434, 9000, 9001
- При обнаружении занятых портов выводится информация о процессах
- Пользователь может решить, продолжать ли установку

**Функция:** `check_ports_available()` в `scripts/auto-install-droplet.sh`

---

### ✅ 3. Миграция базы данных

**Проблема:**
- Миграция запускалась на контейнере `web`, который может не иметь доступа к Prisma CLI
- Не было повторных попыток при неудаче
- Не проверялось, что таблицы действительно созданы

**Решение:**
- Миграция теперь запускается через контейнер `svc-auth`, который гарантированно имеет Prisma
- Добавлено 3 попытки с задержкой 10 секунд между попытками
- После миграции проверяется наличие таблиц в базе данных
- При неудаче выводится инструкция для ручного запуска

**Команда миграции:**
```bash
docker-compose exec svc-auth sh -c 'cd /app && pnpm exec prisma migrate deploy'
```

**Код:** `step10_start_services()` в `scripts/auto-install-droplet.sh`

---

### ✅ 4. Проверка health status контейнеров

**Проблема:**
- Скрипт ждал фиксированное время (60 сек) без проверки готовности контейнеров
- Нет информации о том, какие контейнеры не запустились

**Решение:**
- Добавлена функция `wait_for_healthy_containers()`
- Умная проверка статуса всех контейнеров каждые 10 секунд
- Максимальное время ожидания 5 минут (300 секунд)
- Подсчет unhealthy и starting контейнеров
- Подробный вывод статуса при проблемах

**Функция:** `wait_for_healthy_containers()` в `scripts/auto-install-droplet.sh`

---

### ✅ 5. Автоматическая пересборка web-контейнера

**Проблема:**
- Web-контейнер может не запуститься с первого раза из-за ошибок сборки Next.js
- Нет автоматической попытки пересборки

**Решение:**
- Добавлена проверка статуса web-контейнера после запуска
- При статусе "unhealthy" выводятся логи
- Автоматическая пересборка с флагом `--no-cache`
- Повторный запуск и проверка
- Детальные логи при повторной неудаче

**Код:** В функции `step10_start_services()` в `scripts/auto-install-droplet.sh`

---

### ✅ 6. Проверка доступности порта 3000 перед настройкой nginx

**Проблема:**
- Nginx настраивался до того, как web-сервис начинал отвечать на порт 3000
- Это приводило к ошибкам 502 Bad Gateway

**Решение:**
- Перед настройкой nginx проверяется доступность порта 3000
- 10 попыток с интервалом 5 секунд
- При недоступности выводятся логи web-контейнера
- После настройки nginx проверяется работа проксирования

**Код:** `step11_configure_nginx()` в `scripts/auto-install-droplet.sh`

---

### ✅ 7. Healthcheck для web-контейнера

**Проблема:**
- В `docker-compose.yml` не было healthcheck для web-контейнера
- Docker не мог определить, запустился ли контейнер успешно

**Решение:**
- Добавлен healthcheck для web-контейнера:
  ```yaml
  healthcheck:
    test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
    interval: 10s
    timeout: 5s
    retries: 5
    start_period: 60s
  ```
- Увеличенный `start_period` (60 секунд) дает время для сборки Next.js

**Файл:** `docker-compose.yml` (строки 222-227)

---

### ✅ 8. Улучшенная проверка готовности сервисов

**Проблема:**
- Недостаточная проверка состояния сервисов после установки
- Нет информации о причинах проблем

**Решение:**
- Полностью переписана функция `step16_health_check()`
- Проверка каждого сервиса индивидуально
- Проверка доступности базы данных и наличия таблиц
- Проверка nginx и его проксирования
- Мониторинг дисковго пространства и памяти
- Поиск критических ошибок в логах
- Детальный summary с перечислением проблемных сервисов

**Функция:** `step16_health_check()` в `scripts/auto-install-droplet.sh`

---

## Дополнительные улучшения

### 📖 Руководство по устранению неполадок

Добавлена функция `print_troubleshooting_guide()`, которая выводится при критических ошибках на этапах 10-11.

Руководство включает:
1. Решение проблемы отсутствия таблиц в БД
2. Пересборка web-контейнера при проблемах
3. Устранение проблем с nginx
4. Освобождение занятых портов
5. Диагностика проблем с сервисами
6. Решение проблем с памятью

### 🔍 Улучшенное логирование

- Все критические операции логируются
- Добавлены цветные метки (INFO, SUCCESS, WARNING, ERROR)
- Детальная информация о каждом шаге
- Сохранение всех логов в `/var/log/weddingtech-install.log`

---

## Тестирование

Для проверки исправлений выполните:

```bash
# 1. Проверка синтаксиса скрипта
bash -n scripts/auto-install-droplet.sh

# 2. Проверка docker-compose
docker-compose config

# 3. Симуляция занятого порта
nc -l 3000 &  # В одном терминале
# Запустите скрипт в другом - должно быть предупреждение

# 4. Тест миграции
docker-compose up -d db
docker-compose exec db psql -U pg -d wt -c '\dt'
```

---

## Обратная совместимость

Все изменения обратно совместимы:
- Значения по умолчанию сохранены (`DB_PASSWORD:-pg`)
- Старые .env файлы будут работать
- Существующие установки не затронуты

---

## Команды для ручного исправления проблем

### База данных не имеет таблиц
```bash
docker-compose exec svc-auth sh -c 'cd /app && pnpm exec prisma migrate deploy'
```

### Web-сервис не запускается
```bash
docker-compose logs --tail=50 web
docker-compose build --no-cache web
docker-compose up -d web
```

### Проверка занятых портов
```bash
sudo lsof -i :3000
sudo lsof -i :3001
```

### Проверка статуса всех контейнеров
```bash
docker-compose ps
docker-compose ps --format json | jq '.[]| {name: .Name, status: .Status, health: .Health}'
```

---

## Контрольный список перед запуском

- [ ] Свежий Ubuntu 22.04 LTS
- [ ] Минимум 8GB RAM (или скрипт создаст swap)
- [ ] Минимум 50GB свободного места
- [ ] Права root
- [ ] Порты 3000-3007, 5434, 9000, 9001 свободны
- [ ] Доступ к интернету для загрузки пакетов

---

## Поддержка

При возникновении проблем:
1. Проверьте лог: `cat /var/log/weddingtech-install.log`
2. Проверьте статус: `docker-compose ps`
3. Проверьте логи: `docker-compose logs --tail=100`
4. См. руководство по устранению неполадок выше

---

**Дата последнего обновления:** 2025-10-25  
**Версия:** 2.0  
**Автор:** AI Assistant
